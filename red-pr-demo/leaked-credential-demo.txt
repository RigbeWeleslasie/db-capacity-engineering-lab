# DELIBERATE RED PR -- do not merge.
# This file exists only to prove gitleaks' CI gate actually blocks a commit
# containing a real-shaped credential. Randomly generated so it isn't a
# widely-known "official example" value that scanners specifically
# allowlist (AWS's own docs example key/secret, tried first here, turned
# out to be excluded from gitleaks' own default rules for exactly that
# reason -- too famous to be a real leak signal). This one is not real,
# was never valid, and matches nothing but the SHAPE gitleaks looks for.
AWS_ACCESS_KEY_ID=AKIA7KV8QTEK6OV1SFV3
AWS_SECRET_ACCESS_KEY=ewAsixJ4xtay3x1fr4/erw6jyv6H/SUrKPh1Uuc5
